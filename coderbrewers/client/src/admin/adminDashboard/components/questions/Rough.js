import React from "react";

const Rough = ({ list }) => {
  console.log(list);
  for (var i = 0; i < list.length; i++) {
    console.log(list[i]);
  }
  return <div>Hello</div>;
};

export default Rough;
